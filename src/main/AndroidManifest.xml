<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="samuelpalmer.common">

	<application android:allowBackup="true"
	             android:label="@string/app_name"
	             android:supportsRtl="true"
		>

		<!-- Example declaration of content provider shared preferences -->
		<!--

		<provider
			android:authorities="@string/content_provider_authority"
			android:name="samuelpalmer.common.contentprovidersharedpreferences.SharedPreferencesContentProvider"
			android:exported="false"
			android:process="@string/service_process_name" />

		-->

	</application>

</manifest>
